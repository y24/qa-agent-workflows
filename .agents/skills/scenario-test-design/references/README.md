# Scenario Test Design References

This directory is reserved for scenario-test-design-specific definitions, policies, taxonomies, and decision criteria.

Current detailed guidance is kept in:

- `rules.md`
- `orchestrator.md`
- `steps/`
- `templates/`

Add reference files here when a rule or definition is reused across multiple steps but is too detailed for `SKILL.md`.
